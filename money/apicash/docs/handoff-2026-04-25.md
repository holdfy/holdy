# Handoff — 2026-04-25 (madrugada)

## Estado atual
- Infra local OK (`postgres`, `redis`, `pulsar`) via `../../runinfra.sh` (pasta `money/`).
- Apps OK via `../runapp.sh` (`core` 3000, `admin` 3001, `frontend` 3002, `whatsapp` 3010).
- WhatsApp multi-device **pareado com sucesso** (número normal), sessão persistida no SQLite local.
- Fluxo financeiro E2E via CLI (`apicash-cli test-flow`) executado com sucesso em modo atual.
- Fluxo conversacional WhatsApp avançou até criação de pedido/pagamento.

## Incidentes e mitigação aplicada
- `apicash-whatsapp` teve `stack overflow` em runtime após mensagens recebidas.
- Mitigação operacional aplicada: `RUST_MIN_STACK=16777216` (default em `money/runapp.sh`).
- Após restart, serviço voltou e processou mensagens.

## Limitação importante (agora)
- O PIX gerado no fluxo atual está em **modo mock/sandbox** (`/payments/pix` mock).
- Não é trilha de liquidação bancária real neste estado.

## Objetivo da próxima sessão (manhã)
- Operar com **valores reais** e trilha de dinheiro real passando pelo fluxo.

## Checklist para começar amanhã
1. Confirmar estado dos serviços:
   - `../../runinfra.sh`
   - `../runapp.sh status apicash`
2. Confirmar WhatsApp conectado:
   - `tail -f .runapp/logs/apicash-whatsapp.log`
3. Definir caminho de produção para pagamentos reais:
   - substituir on-ramp/off-ramp mock por integração real (PIX/provider/anchor),
   - validar credenciais reais e ambiente seguro.
4. Executar transação piloto com valor pequeno real (smoke financeiro), com logs e reconciliação.

## Observações de segurança e operação
- Não usar secrets reais em arquivo versionado.
- Manter secrets no `.env` local seguro.
- Registrar ID de transação, timestamps e status de cada etapa para auditoria.
